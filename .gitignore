node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
*.log
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Scratch hand-off files for the /ship agent pipeline (planner/coder/tester/
# reviewer) — regenerated on every run, not meant to be version-controlled.
.pipeline/
